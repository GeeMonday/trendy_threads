<!DOCTYPE html>
<html>
<head>
  <title>Categories Index</title>
</head>
<body>
  <div class="container">
    <h2>Categories</h2>
    <ul>
      <% @categories.each do |category| %>
        <li>
          <%= link_to category.name, category_path(category) %>
        </li>
      <% end %>
    </ul>
  </div>
</body>
</html>
